{
  "name": "projects",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-and-deploy": "npm install && npm run build:prod && ng build deepseek-gui --configuration production"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@infoforum/ifauth-lib": "../ifauth-lib",
    "note": "This is a local library, not installable from npm."
  }
}
