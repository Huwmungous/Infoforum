{
  "dependencies": {
    "angular-auth-oidc-client": "^19.0.0",
    "ifauth-lib": "file:../ifauth-lib"
  },
  "scripts": {
    "build-and-deploy": "npm install && ng build ifauth-lib  --configuration production && ng build deepseek-gui --configuration production",
    "start": "ng serve"
  } 
}
