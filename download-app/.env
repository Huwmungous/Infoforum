# Application name for logging
VITE_IF_APP_NAME=DownloadApp

# Environment (DBG, DEV, SIT, UAT, PRD)
VITE_IF_ENVIRONMENT=DEV

# Log level
VITE_LOG_LEVEL=Debug

# Config service URL (used when not proxied)
# In development, this is proxied via vite.config.js
VITE_IF_CONFIG_SERVICE_URL=/config

# Note: VITE_IF_APP_DOMAIN is no longer needed as an env var
# appDomain is extracted from the URL at runtime:
#   /{appDomain}/downloads/
# Example: /infoforum/downloads/
