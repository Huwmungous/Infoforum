<Window x:Class="ChitterChatterClient.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="ChitterChatter - Login" 
        Height="650" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource IFWindowBrush}"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource IFHeaderBrush}" Padding="12,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Image Source="pack://application:,,,/Resources/IF-Logo.png" Width="32" Height="32" Margin="0,0,12,0" />
                <TextBlock Text="Sign In" 
                           FontSize="18" FontWeight="SemiBold" 
                           Foreground="{StaticResource IFWindowBrush}"
                           VerticalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- WebView2 for Keycloak login -->
        <wv2:WebView2 x:Name="LoginWebView" 
                      Grid.Row="1"
                      NavigationStarting="LoginWebView_NavigationStarting" />

        <!-- Loading overlay -->
        <Border x:Name="LoadingOverlay" 
                Grid.Row="1" 
                Background="{StaticResource IFWindowBrush}"
                Visibility="Visible">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Loading..." 
                           FontSize="16" 
                           Foreground="{StaticResource IFDarkBrush}"
                           HorizontalAlignment="Center" />
                <ProgressBar IsIndeterminate="True" 
                             Width="200" Height="4"
                             Margin="0,12,0,0" />
            </StackPanel>
        </Border>

        <!-- Error overlay -->
        <Border x:Name="ErrorOverlay" 
                Grid.Row="1" 
                Background="{StaticResource IFWindowBrush}"
                Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="24">
                <TextBlock Text="⚠️" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12" />
                <TextBlock x:Name="ErrorText" 
                           Text="An error occurred" 
                           FontSize="14" 
                           Foreground="{StaticResource IFDarkBrush}"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           HorizontalAlignment="Center" />
                <Button Content="Retry" 
                        Click="RetryButton_Click"
                        Style="{StaticResource IFButtonPrimary}"
                        MinWidth="100"
                        Margin="0,16,0,0"
                        HorizontalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" Background="{StaticResource IFHeaderBrush}" Padding="12,6">
            <TextBlock x:Name="StatusText" 
                       Text="Please sign in with your credentials" 
                       Foreground="{StaticResource IFWindowBrush}"
                       FontSize="12"
                       HorizontalAlignment="Center" />
        </Border>
    </Grid>
</Window>
