/**
 * @if/web-common/core - Routing Utilities
 * 
 * Framework-agnostic URL and routing utilities.
 */

export { 
  normalizeBase, 
  getAppBasePath, 
  getCurrentRoutePath, 
  buildAppUrl,
  setDynamicBasePath,
  getDynamicBasePath,
  _setTestBasePath 
} from './AppRoutes';
